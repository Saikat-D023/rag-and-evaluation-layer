name: CI

on:
  push:
    branches: [ "main", "master" ]
  pull_request:
    branches: [ "main", "master" ]

jobs:
  build:
    runs-on: ubuntu-latest

    strategy:
      matrix:
        node-version: [20]

    steps:
      - uses: actions/checkout@v4

      - name: Install pnpm
        uses: pnpm/action-setup@v4
        with:
          version: 9

      - name: Use Node.js ${{ matrix.node-version }}
        uses: actions/setup-node@v4
        with:
          node-version: ${{ matrix.node-version }}
          cache: 'pnpm'
          cache-dependency-path: rag-and-evaluation-layer/pnpm-lock.yaml

      - name: Install dependencies
        working-directory: rag-and-evaluation-layer
        run: pnpm install --frozen-lockfile

      - name: Lint
        working-directory: rag-and-evaluation-layer
        run: pnpm run lint

      - name: Type Check
        working-directory: rag-and-evaluation-layer
        run: pnpm run check-types

      - name: Build
        working-directory: rag-and-evaluation-layer
        run: pnpm run build
